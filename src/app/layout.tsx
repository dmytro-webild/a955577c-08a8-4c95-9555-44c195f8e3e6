import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Open_Sans } from "next/font/google";



export const metadata: Metadata = {
  title: 'WonderGreenLawn | Professional Lawn Care in Houston',
  description: 'Expert lawn care and maintenance services in Houston. Fast, reliable, and highly rated local yard services.',
  keywords: ["lawn care, Houston landscaping, grass cutting, yard service"],
  openGraph: {
    "title": "WonderGreenLawn - Reliable Lawn Care",
    "description": "Professional and affordable lawn care service in Houston, TX.",
    "siteName": "WonderGreenLawn",
    "type": "website"
  },
};

const openSans = Open_Sans({
  variable: "--font-open-sans",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${openSans.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
